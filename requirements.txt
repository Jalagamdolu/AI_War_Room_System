# AI War Room Decision System — Dependencies
# Pure-Python standard library only for core functionality.
# Uncomment optional sections if integrating LLM or ML-based tools.

# ---------------------------------------------------------------------------
# Core (no external deps required for default run)
# ---------------------------------------------------------------------------
# All tools and agents use the Python standard library (json, re, statistics,
# collections, pathlib, argparse, datetime). No pip install needed for baseline.

# ---------------------------------------------------------------------------
# Optional: LLM Integration (e.g. Google Gemini / OpenAI)
# ---------------------------------------------------------------------------
# Uncomment to enable LLM-backed agent reasoning:
#
# google-generativeai>=0.5.0
openai>=1.0.0
# anthropic>=0.25.0

# ---------------------------------------------------------------------------
# Optional: Enhanced Sentiment Analysis (ML-based)
# ---------------------------------------------------------------------------
# Uncomment to replace the rule-based SentimentAnalysisTool with an ML model:
#
# transformers>=4.40.0
# torch>=2.2.0
# vaderSentiment>=3.3.2

# ---------------------------------------------------------------------------
# Optional: Advanced Anomaly Detection
# ---------------------------------------------------------------------------
# Uncomment to augment the MetricsAnalysisTool with statistical libraries:
#
# scipy>=1.13.0
# numpy>=1.26.0
# pandas>=2.2.0

# ---------------------------------------------------------------------------
# Optional: Environment variable loading
# ---------------------------------------------------------------------------
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# Optional: Structured logging
# ---------------------------------------------------------------------------
# rich>=13.7.0   # Beautiful terminal output with colour/panels

# ---------------------------------------------------------------------------
# Development / Testing
# ---------------------------------------------------------------------------
pytest>=8.0.0
pytest-cov>=5.0.0
